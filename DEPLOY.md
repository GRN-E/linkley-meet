# LINKLEY — deploy заавар

Энэ бол **бүрэн төсөл**. Хуучин фолдерээ бүхэлд нь солино. Нэг нэгээр нь файл нэмэх
шаардлагагүй.

---

## АЛХАМ 1 — Хуучин фолдерээ устгах

`~/Downloads/linkley-meet` фолдерийг **хогийн саванд хийнэ**.

> Эхлээд устгахгүй бол macOS шинэ фолдерыг `linkley-meet 2` гэж нэрлэнэ, тэгээд та
> буруу фолдер дотор push хийнэ.

- [ ] Хийсэн

## АЛХАМ 2 — Zip задлах

`linkley-meet.zip`-ийг `~/Downloads` дотор давхар товшиж задална.

Дараа нь Terminal-д нэг мөрөөр:

```bash
cd ~/Downloads/linkley-meet && ls
```

`package.json`, `src`, `index.html`, `public` харагдах ёстой.

> **Хэрэв ижил нэртэй ганц фолдер л харагдвал** zip давхарласан байна. `cd linkley-meet`
> гээд дахин `ls` хий.

- [ ] Файлууд зөв харагдсан

## АЛХАМ 3 — Git холбох

Zip дотор `.git` фолдер **байхгүй**. Тиймээс дахин холбоно. Энэ нь алдаа биш.

```bash
git init
```

```bash
git remote add origin https://github.com/GRN-E/linkley-meet.git
```

> "remote origin already exists" гэвэл алгасна.

```bash
git checkout -b main
```

> "already exists" гэвэл `git checkout main` гэж бич.

- [ ] Хийсэн

## АЛХАМ 4 — Commit ба push

```bash
git add .
```

```bash
git commit -m "New logo, mission-led home page, About page, security fixes"
```

```bash
git push -u origin main --force
```

`--force` энд **зөв**. Таны локал хувилбар бол хүссэн хувилбар.

> Нууц үг асуувал GitHub нууц үг **ажиллахгүй**. Personal Access Token хэрэгтэй
> (`repo` эрхтэй). Terminal-д paste хийхэд юу ч харагдахгүй — энэ хэвийн, зүгээр Enter дар.

- [ ] Push амжилттай

## АЛХАМ 5 — Vercel шалгах

1. vercel.com → төсөл → **Deployments**
2. Дээд мөр **Production** + ногоон чагт (~60 секунд)
3. Сайтыг **нууц цонхонд** нээ (`Cmd + Shift + N`)

> Хуучин хэвээр байвал 99% нь browser cache. Эхлээд нууц цонх шалга.
> **Лого, favicon хамгийн удаан cache-д хадгалагддаг.**

- [ ] Ногоон чагт гарсан

---

# Шалгах жагсаалт

- [ ] Толгойд шинэ **лого** гарсан (үсэг биш зураг)
- [ ] Browser tab дээр шинэ favicon
- [ ] Нүүр: «Мэдлэгээ хуваалцъя, хамтдаа бүтээцгээе»
- [ ] Хар дэвсгэртэй «Мэдлэг бий. Гэхдээ хаалттай.»
- [ ] Цэсэнд: Бидний тухай · Мэргэжилтэн хайх · Төслүүд · Point багц · Мессеж
- [ ] `/about` бүрэн уншигдана
- [ ] Мэргэжилтэн хайхад 9 мэргэжилтэн гарна, `дизаин` гэж буруу бичихэд ч олдоно
- [ ] Төслүүд — 10 төсөл
- [ ] Нэвтэрсний дараа Мессеж хуудас ажиллана, буцах товч бий
- [ ] EN дарвал бүх бичвэр англи болно
- [ ] Утсан дээр цэс, гарчиг эвдрэхгүй

---

# Алдаа гарвал

| Шинж тэмдэг | Шалтгаан ба засвар |
|---|---|
| `fatal: not a git repository` | Zip дотор `.git` байхгүй. АЛХАМ 3-ыг дахин хий. |
| `Updates were rejected` | Түүх зөрсөн. `git push --force` ашигла. |
| Push хийсэн ч сайт өөрчлөгдөөгүй | `main` биш өөр branch руу явуулсан байна. Зөвхөн `main` production руу очдог. |
| Юу ч өөрчлөгдөөгүй мэт | Browser cache. Нууц цонх нээ. |
| Лого хуучин хэвээр | Favicon хамгийн удаан cache-д үлддэг. `Cmd + Shift + R` эсвэл нууц цонх. |

## Сайт эвдэрвэл

Vercel бүх өмнөх хувилбарыг хадгалдаг.

1. vercel.com → **Deployments**
2. Ажиллаж байсан сүүлийн deployment
3. **⋯** → **Promote to Production**

~30 секундэд буцна.

---

# ⚠️ Нэг зүйл өөрчлөх шаардлагатай

`src/components/Footer.jsx` файлын **5-р мөрөнд**:

```js
export const CONTACT_EMAIL = "hello@linkley.tech";
```

Энэ хаяг одоогоор **байхгүй**. Хүн бичвэл хэн ч хүлээж авахгүй. Хоёр сонголт:

1. Google Workspace дээр `hello@linkley.tech` үүсгэх
2. Эсвэл өөрийн ажиллаж байгаа хаягаар солих

Нэг мөр засаад push хийхэд болно.
